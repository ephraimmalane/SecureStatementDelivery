{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Aspire.Hosting.Dcp": "Warning"
    }
  },
  "_Parameters_comment": [
    "Secret parameter VALUES are NOT stored here. They resolve from user-secrets (local dev)",
    "or the platform secret store (production). To seed local dev, run from the AppHost project:",
    "  dotnet user-secrets set \"Parameters:keycloak-admin-password\" \"admin\"",
    "  dotnet user-secrets set \"Parameters:keycloak-client-secret\" \"<32+ char secret>\"",
    "  dotnet user-secrets set \"Parameters:download-token-secret\" \"<32+ char secret>\"",
    "If a parameter is unresolved at run time, the Aspire dashboard will prompt for it."
  ]
}
