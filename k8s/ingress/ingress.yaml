# Prerequisites: nginx ingress controller and cert-manager must be installed.
# Replace api.secure-statements.example.com with your actual domain.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-api-ingress
  namespace: secure-statements
  labels:
    app.kubernetes.io/name: web-api
    app.kubernetes.io/part-of: secure-statement-delivery
  annotations:
    # TLS
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"

    # Security headers (supplement app-level headers set by MiddlewareExtensions.cs)
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "Strict-Transport-Security: max-age=31536000; includeSubDomains; preload";

    # Request body size limit — matches Kestrel's MaxRequestBodySize and the domain validator (50 MB).
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"

    # Upload timeout: 50 MB at a minimum enterprise upload speed of 10 Mbps takes ~40 s.
    # 300 s accommodates 1 Mbps connections without hanging indefinitely on stalled transfers.
    # Download timeout: local-storage path streams through the API; S3 path is a presigned
    # redirect so the API sends only a 302 — no data transfer time involved.
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "10"

    # Rate limiting — supplements the in-process ASP.NET Core rate limiter
    nginx.ingress.kubernetes.io/limit-rps: "20"
    nginx.ingress.kubernetes.io/limit-connections: "10"

    # Proxy real client IP through to the application
    nginx.ingress.kubernetes.io/use-forwarded-headers: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.secure-statements.example.com
      secretName: web-api-tls
  rules:
    - host: api.secure-statements.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-api
                port:
                  number: 8080
